# Task 12 — Color Toggler

Simple demo toggling the scaffold background color using a FloatingActionButton.

Run:

```bash
cd /workspaces/madlab-pracs/task_12
flutter pub get
flutter run -d web-server
```
