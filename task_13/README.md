# Task 13 — AnimatedContainer

Simple demo showing an `AnimatedContainer` that animates size and color changes triggered by a FloatingActionButton.

Run:

```bash
cd /workspaces/madlab-pracs/task_13
flutter pub get
flutter run -d web-server
```
