name: task_7
description: Read and display data from local JSON string
publish_to: 'none'
version: 1.0.0
environment:
  sdk: ">=3.10.0-0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
