# Task 5 — Fade-In Animation

Simple Flutter demo showing an implicit fade-in using `AnimatedOpacity`.

How to run:

```bash
cd /workspaces/madlab-pracs/task_5
flutter pub get
flutter run -d web-server
```
