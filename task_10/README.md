# Task 10 — Draggable & DragTarget

This demo shows a simple Draggable widget and a DragTarget. Drag the box into the target area to change the target's color and text.

Run:

```bash
cd /workspaces/madlab-pracs/task_10
flutter pub get
flutter run -d web-server
```
